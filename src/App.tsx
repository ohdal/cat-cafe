function App() {
  return (
    // The visible bottom bar. The area *above* this window is desktop and stays
    // fully clickable — see position_bottom_bar() in src-tauri/src/lib.rs.
    <main className="flex min-h-screen flex-col items-center justify-center rounded-t-2xl bg-neutral-900/85 text-neutral-100">
      {/* cat-cafe UI goes here */}
    </main>
  );
}

export default App;
